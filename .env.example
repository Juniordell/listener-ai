# Whisper model size: tiny, base, small, medium, large
WHISPER_MODEL=base

# LLM settings (for local dev without Docker — points to local Ollama)
LLM_BASE_URL=http://localhost:11434/v1
LLM_API_KEY=ollama
LLM_MODEL=llama3.2
